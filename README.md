# Amazon URL Cleaner

This Chrome extension automatically removes metadata from Amazon product links. Currently it only supports `amazon.co.jp`.

## Installation

Generate a minified build as follows:

```sh
npm run build
```

Then, open `chrome://extensions` and add the artifacts above.

Don't forget to turn on Developer Mode.
